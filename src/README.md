# GeoTracker

A lightweight geolocation tracking library.

## Installation

```bash
npm install geo-tracker
```

## Usage

```javascript
import { GeoTracker } from 'geo-tracker';

const tracker = new GeoTracker();
tracker.start();
```